# 📁 backend/.env.production
# ✅ Baseado em .env.example existente + ajustes para produção

# Autenticação
JWT_SECRET=um-segredo-seguro-em-producao
REGISTER_TOKEN=chave-superadmin-prod

# Banco de dados (conforme docker-compose)
DB_HOST=postgres
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=multi_tenant_db

# Backend
PORT=3000

# CORS permitido para o frontend rodando via Docker local ou domínio
CORS_ORIGIN=http://localhost:5173
